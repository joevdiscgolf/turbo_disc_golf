/// Enum for specifying how a stat card should be rendered in the story tab
enum StatRenderMode {
  /// Render as a circular indicator (88px) - More visual impact
  circle,

  /// Render as a horizontal progress bar - Saves vertical space
  bar,
}
